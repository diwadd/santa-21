#pragma once

#include <bits/stdc++.h> 

using namespace std;

void run_metropolis(vector<int> &initial_state,
                    vector<vector<int>> &distance_matrix,
                    int k_max = 100);