#pragma once

#include <bits/stdc++.h>

using namespace std;

struct MarkedPermutationsLimits {
    int start = -1;
    int stop = -1;
};

ostream& operator<<(ostream& os, const MarkedPermutationsLimits & mpl);